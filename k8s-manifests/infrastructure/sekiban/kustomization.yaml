apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - helmrelease.yaml

labels:
  - pairs:
      app.kubernetes.io/component: admission-webhook
      app.kubernetes.io/part-of: tameshi
    includeSelectors: false
    includeTemplates: true
